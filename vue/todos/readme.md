# Todos 任务清单 VUE开发思路

- 响应式数据驱动的界面开发
  v-if v-model v-for 
  composition API
  @keydown.enter
  :class
  computed
  Vue开发方式 简单 高效 好上手

- 传统做法
  先找到dom元素，然后对其修改(dom)
- VUE 做法
  不再需要思考页面的元素怎么操作，而是要思考数据是怎么变化的
- v-指令
  v-for 循环输出
- computed 计算属性
  性能更好 只有在依赖数据变化时,才会重新计算 
