## OOP 面向对象
Object Orient Programming 面向对象编程

## 封装 
JS 是一种**基于**对象（Object-base）的语言，你遇到的几乎所有东西都是对象(连简单数据类型都有包装类)。
他有不是一种真正的面向对象，早期连class关键字都没有(哪怕es6有了class，仍然是原型式的面向 对象)

- 如果没有Constrnctor 没有class，怎么做面向对象？
  用对象字面量

## 生成实例对象的原始模式
- 实例生成的过程
  - 函数以new的方式调用
  - 函数内部创建空对象，this指向这个空对象
  - 执行构造函数里面的代码，空对象就有了类模版的属性了
  - 返回这个实例

  对象字面量虽好，但是我们还是要封装一下，生成实例对象的过程，Constructor

## Prototype模式
把不变的属性和公用的方法，都放到原型对象上

